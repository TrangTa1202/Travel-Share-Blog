const User = require('./Users');
const Post = require('./Posts');
const Comment = require('./Comments');
const VerifyCode = require('./VerifyCode');

module.exports = {
  User, Post, Comment, VerifyCode,
};
